export enum ConfigTypeCodeMap {
    OBJECT_AND_CHANGED = 'OBJECT_AND_CHANGED',
    OBJECT_ONLY = 'OBJECT_ONLY',
}

export enum FormMode {
    CREATE = 'CREATE',
    EDIT = 'EDIT',
}

export enum TemplateContentType {
    TEXT = 'TEXT',
    HTML = 'HTML',
}

export enum ChannelType {
    EMAIL = 'Email',
    TELEGRAM = 'Telegram',
    IN_APP = 'InApp',
}

export enum ChannelTypeMap {
    TELEGRAM = 'TELEGRAM',
    EMAIL = 'EMAIL',
    FILE = 'FILE',
}

export enum ReceiverField {
    EMAIL = 'emailReceivers',
    TELEGRAM = 'telegramReceivers',
}

export enum ObjectConfigType {
    ObjectAndChanged = 'OBJECT_AND_CHANGED',
    ObjectOnly = 'OBJECT_ONLY',
}

export enum ContentType {
    Html = 'HTML',
    Text = 'TEXT',
}
