export enum TIMELINE_TYPE {
    HOUR = 0,
    DAY = 1,
}

export enum SortEnumType {
    Asc = 'ASC',
    Desc = 'DESC',
}

export enum DataGridSortType {
    Asc = 'ASC',
    Desc = 'DESC',
    Ready = 'READY',
}

export enum DirectoryRoot {
    Directory = 'directory',
    Place = 'place',
    Workflow = 'workflow',
    Workflow_gate = 'workflow_gate',
    Workflow_scheme = 'workflow_scheme',
    Menu = 'menu',
    Storage = 'storage',
    Report = 'report',
    Group = 'group',
    Campaign = 'campaign',
    Page = 'page',
    PageWelcome = 'pageWelcome',
    PageLogin = 'pageLogin',
    PageGadget = 'pageGadget',
    PageHistory = 'pageHistory',
    CampaignCurrent = 'campaignCurrent',
    CampaignHistory = 'campaignHistory',
    Template = 'template',
    Layout = 'layout',
    Authentication_profile = 'authentication_profile',
    Archive = 'archive',
    Ap = 'ap',
    Place_group = 'place_group',
    Wizard = 'wizard',
    Campaign_collection = 'campaign_collection',
    Task_scheduler = 'task_scheduler',
}

export enum WorkspaceType {
    AXN = 'AXN',
    ADMIN = 'ADMIN',
    ACM = 'ACM',
}
